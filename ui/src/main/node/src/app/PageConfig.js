import React from "react";
import classNames from "classnames";
import jss from "./PageConfig.jss";

const PageConfig = ({classes}) => (
    <div className={classNames(classes.root)}>
        {/*<div/>*/}
    </div>
);

export default jss(PageConfig);
